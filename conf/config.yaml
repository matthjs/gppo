defaults:
  - mode: train  # Set a default mode value, it can be overridden by command-line args
  - agent: ppo
  - _self_

# Common hyperparameters (can be overridden)
num_episodes: 50
num_bootstrap_samples: 100
num_runs: 1
results_save_path: "./results/"
environment: "Walker2d-v5"   # "InvertedPendulum-v5"   Walker2d-v5

wandb:
  project: "gppo-drl"
  entity: "ml_exp"
  use_wandb: True

# Hydra plugin settings
# hydra:
#   run:
#     dir: outputs/${mode}
#   sweep:
#     dir: multirun/${mode}
#    subdir: hpo