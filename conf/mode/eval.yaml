name: eval

execute: True
load_model: True
save_model: False
model_path: "../models"   # e.g., /models/gppo/file.pth
import_metrics: False
export_metrics: True
eval_episodes: 10
eval_runs: 1
# List of agents to import/enable
agents:
  - GPPO
  - SB_PPO

normalize_obs: True
clip_obs: 10.0 #1.0   #
